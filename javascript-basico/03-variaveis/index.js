var variavelGlobal  = "variavel mutavel de escopo global";

const variavelConstante = "variavel constante, nao pode ser alterada após criada";

let variavelEscopoLocal = "variavel mutavel de escopo local";