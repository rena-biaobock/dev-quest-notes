enum Colors {
    Red,
    Blue,
    Green
}

function showColor(color) {
    console.log(color)
}

showColor(Colors.Red)

enum TradeType {
    ACAO = 9281,
    TESOURO_SELIC = 6554
}
 
TradeType.ACAO